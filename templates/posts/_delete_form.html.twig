<form method="post" action="{{ path('app_posts_delete', {'slug': post.slug}) }}" onsubmit="return confirm('êtes vous sur de vouloir supprimer cette publication?')">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ post.id) }}">
    <button class="btn btn-outline-danger">Supprimer</button>
</form>
